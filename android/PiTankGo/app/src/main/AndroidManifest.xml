<!--ARCHIVO DE CONFIGURACIÓN DE UNA APP ANDROID -->
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="upm.sdg2.pitankgo">

    <!--Declaramos el permiso de uso de internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--Definimos el icono y el nombre de la app 
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
    
        Permitimos el trafico en http:
        android:usesCleartextTraffic="true"

        Forzamos la vista horizontal de GameActivity:

        <activity android:name=".GameActivity"
            android:screenOrientation="landscape"></activity>-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".GameActivity"
            android:screenOrientation="landscape"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>